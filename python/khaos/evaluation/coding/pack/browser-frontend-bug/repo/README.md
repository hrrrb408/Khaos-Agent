# Browser frontend bug fixture

The Save button is intentionally wired to the wrong semantic target. The
agent should preserve the small static app and make the status transition
observable to a browser check.
