# M6 Security Architecture Closure Report

Status: **NOT CLOSED (evidence incomplete)**

This report is evidence-bound. A local green test run is not a closure claim.

- Exact commit: `94c39b9e0601cb49b4d7519d24748725adcce980` (review baseline)
- Remediation state: working-tree remediation is uncommitted; regenerate this
  report against the final protected-main commit before claiming closure.
- CI run evidence: `not-provided`
- Full test counts: `not-provided`
- Native evidence: `UNKNOWN`
- All required gates explicitly successful: `False`
- Verified evidence bundle: `UNKNOWN (no verified evidence bundle provided)`

## Finding closure matrix

| Finding | Root cause | Invariant | Modified files | Exploit before | Why impossible after | Tests | Evidence / limitation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| M6.1 Shell semantic authority | Shell safety was previously inferred from token splitting and executable names. | Only a complete literal executable graph is SAFE; expansion or callback ambiguity requires approval. | `python/khaos/security/shell_semantics.py; python/khaos/permissions; python/khaos/tools/terminal_tools.py` | A shell expansion or callback could make the executed graph differ from the approved base command. | The immutable semantic AST binds expansions, pipelines, callbacks, redirections, and nested evaluators; unknown is not read-only. | `test_shell_semantics.py; M6 adversarial matrix` | Local source-level; Linux/host execution remains CI-bound. The parser is conservative and does not claim to be a shell interpreter. |
| M6.2 Native authority transports | Native platforms lacked a production OS-owned authority transport with distinct identity proof. | Missing service identity, peer identity, protected key, or transport proof fails closed; no same-UID Python authority. | `python/khaos/security/native_authority.py; packaging/macos; packaging/windows; rust/khaos-core/src/bin/khaos-authorityd-windows.rs` | A platform adapter could be absent or simulated in the agent process. | macOS launchd/XPC and Windows Service-SID/Named Pipe contracts require external native proof and protected-key configuration. | `test_native_authority.py; cross-target Rust compile; native-authority-production-e2e.yml` | Native E2E is CI/manual evidence and is UNKNOWN until real artifacts are supplied. This workstation cannot produce hosted macOS/Windows service evidence. |
| M6.3 Pure TCB boundaries | Protocol serialization, lifecycle transitions, and orchestration were distributed across stateful services. | Canonical serialization, schema validation, receipt transitions, owner transitions, and effect binding are pure typed functions. | `python/khaos/security/protocol_boundary.py; docs/adr/ADR-021-pure-tcb-boundaries.md` | Implicit object state could be mistaken for a valid receipt or closed resource. | Illegal transitions, unknown schema fields, and non-empty CLOSED ownership are rejected by pure boundary primitives. | `test_protocol_boundary.py with Hypothesis` | Local property-based source-level evidence. The external kernel/oracle portions remain platform CI responsibilities. |
| M6.4 Typed principal delegation | Different ingress paths could converge on an underspecified principal identity. | Principal kind, parent, project, session, runtime, task, workspace, operation, resource, policy, expiry, and nonce stay bound; delegation is narrow-only. | `python/khaos/security/principals.py; python/khaos/runtime/context.py; python/khaos/security/authorityd_protocol.py` | A receipt or child task could be replayed across principals or sessions if only a principal id was checked. | Typed fields are carried through the receipt and execution plan; production authority rejects untyped or cross-session reuse. | `test_typed_principals.py; runtime and subagent regressions` | Local authority protocol evidence. A transport-root digest is an identity commitment, not a substitute for an external signed human delegation. |
| M6.5 Production reachability | Manual inspection could miss a dev-only or host fallback reachable through a transitive import. | Generated production graph has no unresolved or forbidden edges from every composition root. | `scripts/generate_production_reachability.py; docs/generated/production-reachability.md` | A forbidden backend could be reachable even when no obvious direct call site used it. | The source-derived graph fails on unresolved modules or forbidden production targets. | `test_production_reachability.py; security-contract-matrix.yml` | Generated local graph; exact-commit CI rerun required for release. Static import reachability does not prove every runtime branch or kernel property. |
| M6.6 Release governance preparation | The current repository has one maintainer and cannot truthfully claim independent review. | The hardened second-maintainer template requires approval, code-owner review, last-push approval, resolved threads, and both aggregate gates. | `.github/CODEOWNERS; scripts/github-m6-hardened-ruleset.json; scripts/validate_m6_governance.py` | A sole maintainer ruleset could not provide independent review evidence. | Security-critical ownership and a machine-validated hardened template are ready, while current evidence remains explicitly single-maintainer. | `test_m6_governance.py` | Repository configuration preparation only; independent human review is external. Do not enable the hardened template until a second authorized maintainer exists. |
| M6.7 Upstream Codex security watch | The fixed upstream baseline was not automatically checked for new security-relevant metadata. | Watch output is review-only metadata; it cannot copy, apply, or synchronize upstream source. | `scripts/watch_upstream_codex_security.py; .github/workflows/upstream-codex-security-watch.yml; docs/upstream-codex.md` | A floating upstream HEAD could silently change the security comparison baseline. | The current SHA is fixed and the scheduled watcher emits semantic review candidates only. | `test_upstream_codex_security_watch.py` | Remote metadata watch; candidate assessment is a human review step. An empty candidate list is not a proof that Khaos is secure. |
| M6.8 Final adversarial closure | Attack surfaces were covered by separate tests without one postcondition-oriented matrix. | SUCCESS requires exact effect; CLOSED requires independent terminal resource proof; otherwise state is failed, unknown, or quarantined. | `python/tests/security/test_m6_adversarial_matrix.py; python/khaos/security/protocol_boundary.py` | A return code or local object state could be mistaken for physical cleanup or exact effect. | The matrix checks negative postconditions across replay, mutation, path races, process, network, lifecycle, and fallback boundaries. | `M6 adversarial matrix plus existing real-kernel/resource-owner suites` | Local matrix and CI-only kernel/native evidence are kept separate. No local report may claim CLOSED without the required native and exact-commit CI artifacts. |
| M6.9 Real security closure | Native frontends forwarded to backends that did not exist on macOS/Windows; the transport probe stood in for an E2E; proofs were static digests; delegation digests were caller-supplied; closure was a CLI attestation; git branch argv matched by name; native CI assumed state runners do not have. | Native authority proofs are signed challenge-responses bound to designated code requirements; delegations are authority-issued and one-shot; closure requires verified evidence manifests; SAFE argv semantics are proven, not name-matched. | `ADR-022/023; authorityd.py; authorityd_windows.py; native_authority.py; principals.py; delegation_issuer.py; security_evidence.py; shell_semantics.py; native-authority-production-e2e.yml; production_composition_manifest.py` | A transport probe, two local files, or a parent digest could all masquerade as security evidence. | Real frontend/backend chains, signed attestations, authority-owned delegation registry, provenance-verified closure, per-command argv proofs, real CI provisioning. | `test_native_authority_backend/attestation; test_delegation_authority_owned; test_security_evidence_provenance; test_argv_semantic_policy; test_windows_authority_lifecycle; test_production_composition_manifest; test_tcb_boundaries` | Local fail-closed tests green; hosted macOS/Windows real E2E remains CI evidence (UNKNOWN until artifacts exist). Native runners and release SHA evidence are external; this report stays NOT CLOSED without them. |

## Evidence classification

- **Local:** pure protocol, identity, parser, graph, and negative tests executed on this workstation.
- **CI-only:** Linux namespaces/cgroups/nftables, Docker, full product matrices, and hosted platform tests.
- **Native CI/manual:** real launchd/XPC and Windows Service-SID/Named Pipe artifacts; mock output is never accepted.
- **Unknown/external:** independent human review, remote WORM audit, and any missing native artifact.

The report must remain NOT CLOSED whenever an independent resource oracle, exact approved effect proof, native proof, or required CI run is missing.
